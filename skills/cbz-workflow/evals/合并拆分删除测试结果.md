# 合并、拆分与删除子合集测试结果

- 测试日期：2026-09-03
- Python：3.11.9 64 位
- 测试对象：自动生成的隔离微型 CBZ，不含真实漫画

## 可逆合并与拆分

- [x] 输入顺序打乱后，仍按第 1 话、第 2 话自然排序；
- [x] 两个来源 CBZ、共 4 张正文图片成功合并；
- [x] 合集封面计入后，元数据页数正确写为 5；
- [x] 合集 ZIP 完整校验通过；
- [x] 可逆拆分清单生成并可读取；
- [x] 成功还原两个来源 CBZ；
- [x] 还原后的成员名称、文件内容和 ZIP 注释与原文件逐项一致。

## 删除子合集

- [x] 正确识别 3 个数字分组；
- [x] 删除第 2 组后保留第 1 组；
- [x] 原第 3 组正确重排为第 2 组；
- [x] 被删除分组的内容没有残留；
- [x] 图片页数由 3 更新为 2；
- [x] `ComicInfo.xml` 中的 `PageCount` 更新为 2；
- [x] 输出 ZIP 完整校验通过；
- [x] 原始测试合集保持不变，结果写入新文件。

## 删除新版可逆合集中的子合集

- [x] 用现有可逆合并器生成 3 本隔离微型 CBZ 的合集；
- [x] 删除第 2 组后，`META-INF/cbz-merge-manifest.json` 的来源记录由 3 条变为 2 条；
- [x] 被删除来源的记录和 `.cbz-restore` 数据没有残留；
- [x] 原第 3 组的分组路径、来源位置和还原路径正确重排为第 2 组；
- [x] 清单中的全部 `mapped`、`stored` 路径均可在输出 CBZ 中找到；
- [x] 使用现有拆分器成功还原剩余第 1 本和第 3 本；
- [x] 两本还原结果的成员名称、文件内容和 ZIP 注释与原文件逐项一致；
- [x] 修改后的 Python 脚本通过语法检查。

## 删除工具 ZIP64 兼容性修复（2026-09-05）

- [x] 普通成员不再无条件使用 `force_zip64=True`；
- [x] 来源文件中遗留的非必要 ZIP64 版本标记不会复制到新文件；
- [x] 仍由 Python 根据真实成员大小自动启用 ZIP64；
- [x] 小型隔离样本删除后 ZIP、CRC、图片、XML 和可逆清单验证通过；
- [x] 旧版生成的真实 CBZ 可无损重新打包为普通 ZIP 成员。

## 尚未覆盖

- 超过 2 GB 的 CBZ；
- 数百或数千页的大合集；
- 真实漫画副本；
- 图形界面文件选择操作；
- JM 分卷完整性报告；
- JMComic 下载与打包。
