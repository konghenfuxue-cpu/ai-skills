# 安全策略

## 支持范围

安全修复优先应用于 `main` 和最新 GitHub Release。旧标签用于复现历史版本，不保证继续维护。

## 报告安全问题

请不要在公开 Issue 中发布 Token、Cookie、账号、私有书库路径、真实书籍内容或可直接利用的敏感细节。

优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。如果该入口暂不可用，请创建不含敏感细节的公开 Issue，仅说明“需要私密安全沟通”。

报告建议包含：

- 受影响的 Skill、脚本和版本；
- 可复现的最小步骤；
- 可能造成的文件覆盖、数据泄露或命令执行影响；
- 已尝试的缓解方法；
- 不含真实书籍或凭据的隔离样本。

## 安全边界

- 本项目不处理或绕过 DRM。
- 不应把真实漫画、电子书、Calibre 数据库、Cookie、Token 或 API Key提交到仓库。
- 文件删除、覆盖和网络下载必须由用户明确触发。
- Calibre 审计默认只读，不直接写入 `metadata.db`。
- 发现可能泄露的凭据时，应立即撤销或轮换；仅从最新提交删除并不能清除 Git 历史。
