# 数字书库与漫画 Agent Skills：公开版安装与使用教程

本教程适用于 `ai-skills` 的公开稳定版本 `v1.0.0`。仓库可匿名访问，安装时不需要 GitHub 账号。

## 一、包含的 Skill

| Skill | 用途 | 状态 |
|---|---|---|
| `cbz-workflow` | CBZ 检查、元数据、合并、拆分与删除子合集 | 稳定（本地核心） |
| `epub-repair` | EPUB 结构检查与安全包装修复 | 稳定 |
| `calibre-workflow` | Calibre 书库只读审计 | 稳定 |
| `skill-repository-manager` | Skill 仓库检查、测试、提交与同步 | 稳定 |

## 二、准备环境

- Windows 10 或 Windows 11；
- PowerShell 7；
- 64 位 Python 3.11 或更高版本；
- Codex 用户 Skill 目录：`%USERPROFILE%\.codex\skills`。

CBZ、EPUB 和 Calibre 的本地核心脚本只使用 Python 标准库。只有 JMComic 下载与打包功能需要额外安装：

```powershell
python -m pip install jmcomic Pillow zhconv
```

## 三、下载固定稳定版

推荐安装固定的 `v1.0.0`，避免以后 `main` 更新导致行为变化。

### 方法 A：使用 Git

```powershell
git clone --branch v1.0.0 --depth 1 https://github.com/konghenfuxue-cpu/ai-skills.git
Set-Location '.\ai-skills'
```

### 方法 B：下载 ZIP

打开以下地址并解压：

<https://github.com/konghenfuxue-cpu/ai-skills/archive/refs/tags/v1.0.0.zip>

## 四、安装到 Codex

在仓库根目录运行：

```powershell
$skillsTarget = Join-Path $env:USERPROFILE '.codex\skills'
New-Item -ItemType Directory -Force -Path $skillsTarget | Out-Null
Copy-Item -Recurse -Force '.\skills\cbz-workflow' (Join-Path $skillsTarget 'cbz-workflow')
Copy-Item -Recurse -Force '.\skills\epub-repair' (Join-Path $skillsTarget 'epub-repair')
Copy-Item -Recurse -Force '.\skills\calibre-workflow' (Join-Path $skillsTarget 'calibre-workflow')
Copy-Item -Recurse -Force '.\skills\skill-repository-manager' (Join-Path $skillsTarget 'skill-repository-manager')
```

只需要某个 Skill 时，只执行对应的 `Copy-Item`。安装后重新启动 Codex。

## 五、在 Codex 中调用

### 检查 CBZ

```text
使用 $cbz-workflow 检查 "D:\Books\comic.cbz" 的 ZIP、图片页数和 ComicInfo.xml，只报告问题，不修改原文件。
```

### 合并或拆分 CBZ

```text
使用 $cbz-workflow 分析这些 CBZ，按自然顺序可逆合并到新文件。保留原文件，完成后验证 ZIP、页数和元数据。
```

```text
使用 $cbz-workflow 检查这个可逆合集，并拆分还原到新目录，不覆盖现有文件。
```

### 删除合集中的一项

```text
使用 $cbz-workflow 分析这个合集中的第 2 项。先显示名称、图片数和范围，确认后再生成删除结果，不覆盖原文件。
```

### 检查或修复 EPUB

```text
使用 $epub-repair 检查 "D:\Books\book.epub"，只报告结构问题，不修改原文件。
```

```text
使用 $epub-repair 修复这个 EPUB 的包装层，生成新文件并重新检查，不覆盖原文件。
```

### 审计 Calibre 书库

```text
使用 $calibre-workflow 只读审计 "D:\Calibre Library"，生成 JSON 报告，不修改 metadata.db 或书籍文件。
```

## 六、直接运行只读工具

在仓库根目录运行：

```powershell
python '.\skills\epub-repair\scripts\epub_check_repair.py' 'D:\Books\book.epub'
python '.\skills\calibre-workflow\scripts\audit_calibre_library.py' 'D:\Calibre Library' --report '.\calibre-audit.json'
& '.\skills\skill-repository-manager\scripts\check-skill-repo.ps1'
```

这些示例是只读检查。EPUB 加上 `--repair` 时会创建“原名_已修复.epub”，不覆盖原文件。

## 七、安全规则

1. 第一次处理真实文件时使用副本。
2. 合并、拆分和修复默认输出新文件；操作后检查 ZIP、页数、XML 和阅读器兼容性。
3. 删除子合集前核对编号、名称、页数和范围。
4. 不直接修改 Calibre 的 `metadata.db`，整理动作优先使用 Calibre 自身功能。
5. 不将漫画、电子书、Cookie、Token、账号凭据或下载日志提交到公开仓库。
6. 本项目不提供 DRM 绕过功能。

## 八、更新与卸载

更新时下载新的版本标签，先查看发布说明，再将需要的 Skill 目录重新复制到 Codex。不要把 `main` 当作固定稳定版本。

卸载时关闭 Codex，仅删除 `%USERPROFILE%\.codex\skills` 下对应的个人 Skill 文件夹；不要删除整个 `.codex\skills` 目录，因为其中可能还有其他 Skill。

## 九、更多说明

- [CBZ Workflow 详细说明](CBZ-Workflow使用说明.md)
- [Skill 仓库管理说明](Skill仓库管理使用说明.md)
- [EPUB Repair](skills/epub-repair/SKILL.md)
- [Calibre Workflow](skills/calibre-workflow/SKILL.md)
- [v1.0.0 源码标签](https://github.com/konghenfuxue-cpu/ai-skills/tree/v1.0.0)
